# Scratch / build artifacts.
__pycache__/
*.pyc
data/raw/
# OpenEvolve scratch run output. The REPORTED campaign lives in results/
# (baseline_test.json, _pool_124_125/, oe_island_*/best/). oe_output/ is a generation-0
# scratch run and must not be mistaken for the campaign.
openevolve/oe_output/
# (Processed data arrays are handled by data/.gitignore: raw/ and processed/*.npy are
# regenerated by build_data.py; only data/processed/metadata.json -- the SHA256-pinned
# provenance contract -- is tracked.)
